// A ruled "fill-in" field — a baseline underline rather than a boxed input, so
// the forms read like a specimen card being annotated, not a SaaS form. The
// clear affordance in ClearableInput reserves the right padding.
export const inputClass =
  "w-full border-b border-line-strong bg-transparent py-1.5 text-[15px] text-ink outline-none transition-colors placeholder:text-ink-faint focus:border-accent";
